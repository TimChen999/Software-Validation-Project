python3 -m venv .venv
source .venv/bin/activate (Windows: .\.venv\Scripts\activate.bat)
pip3 install -r requirements.txt

To execute:
python3 main

(Note: main.py interacts with LM studio, make sure to have it running with the correct models installed)